%Script for setting parameters of data generating distribution
%The data generating distributions are gaussian
%Run this script in the command line

p1=0.5;
p2=0.5;
mu1=[0;0];
mu2=[2;0];
sigma1=eye(2,2);
sigma2=eye(2,2);
sampleCount=100;